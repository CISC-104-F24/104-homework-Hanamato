using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     Debug.Log("Hello World!");   
     Debug.LogWarning("Hello, I am a warning");
     Debug.LogError("Error!");

     //Author: Hannah A
     /*
      How complicated is the coding going to get? Adding characters, collisions, etc
      A simply game where you can explore environments and solve puzzles could be fun to try to make
      I want to find out more about how you would code character interactions, whether its dialogue tress or how npcs know how to respond to actions the player makes in the overworld
     */  
    }

    // Update is called once per frame
    void Update()
    {
    }
}
