using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Environment : MonoBehaviour
{
    public float playerHealth = 100f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

       //public bool isPlayerDead (playerHealth = 0f)
        

    }

    /*private void OnCollisionEnter (Collision collision);

        Void OnCollisionStay (Collision collision);
        if collision.gameObject.name = "Lava pool"
        {

        }

        if (collision.gameObject.name == "Rising Platform")
        {
            transform.position = transform.position + new Vector3 (0,3,0) * baseSpeed * Time.deltaTime;
        }
            //Rigidbody myRigidBody = GetComponent<Rigidbody>();
            //myRigidBody.AddForce(new Vector3(0,10,0), ForceMode.Impulse);
            positionChange = new Vector3(0f,1f,0f); 
            */
}